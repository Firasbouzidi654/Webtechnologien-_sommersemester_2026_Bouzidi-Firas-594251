# Local development works with `docker compose up -d db` without this file.
# For Render, add the real values in the Render dashboard only.

DATABASE_URL=jdbc:postgresql://YOUR_RENDER_INTERNAL_HOST:5432/YOUR_DATABASE
SPRING_DATASOURCE_USERNAME=YOUR_RENDER_USERNAME
SPRING_DATASOURCE_PASSWORD=YOUR_RENDER_PASSWORD
SPRING_PROFILES_ACTIVE=prod
PORT=10000

# Used only when building the frontend on Render.
VITE_API_BASE_URL=https://your-backend.onrender.com
